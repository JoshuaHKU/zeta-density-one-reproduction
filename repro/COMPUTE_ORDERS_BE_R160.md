# 分支相等攻坚第 3 轮 · 计算指令书（O-BE-4/5/6）
# ——证明已成形，本轮任务全部是核验层：证伪保险 + 数据充分性

数学：Claude；纲领指挥：Hongyi Yang。
背景：r160 备忘（max_than_0.7945/r160/branch_equality_round3.md）。
引理 L/L′、逐项多项式定理 P1、逐项宇称定理 P2 已有完整证明 +
沙盒机械核验（b ≤ 5）。**任何一个违例都推翻证明——真火警立报。**
判据先记后算；原始日志只增不改。

---

## O-BE-4：提升系统 L′ 全子式证伪扫描（优先 1）

**对象**。在 O-BE-1 的墙系统上加 c-列：坐标 = (m_0..m_{b−1},
c_B 每块一个)。行：
1. m-单位行 e_i（全部 i）；
2. 整块等式行 ∂B = Σ_{i∈B}(e_i − e_{i+1 mod b})（无 c-分量）；
3. 提升前缀行 e_{c_B} + ∂S，S 取块 B 的全部 σ_B-前缀
   （含整块本身）。
即 b + r + b 行 × (b + r) 列（r = 块数）。

**任务**。b = 5, 6, 7：枚举全部 (分拆, 逐块排列)，构造提升
矩阵，按内容规范形 + ι-反射去重，**全部 k×k 子式（k ≤ b+r）
∈ {0,±1}**（Bareiss 精确整数）。违例即停、出证人。

**判据（先记）**：F-BE-LTU-b = "b 级全部提升矩阵零违例"。
沙盒已验 b=3/4 全量（13/73 系统）PASS。成本预计与 O-BE-1
同数量级偏上（列数 +r）；b=7 沿用 24 分片双机方案，先标定。

## O-BE-5：逐 σ-项多项式 + 宇称独立扫描（优先 2）

**对象**。逐 (P, σ⃗) 项的直接 span-乘积计数（**不走累积量
Möbius 层**，与 O-BE-2 互补）：

    T_{P,σ⃗}(N) = Σ_{m⃗∈[0,N)^b} ∏_B δ[Σ_{i∈B} k_i = 0] ·
                  (N − span_{σ_B}(k_B))₊ ，  k_i = m_i − m_{i+1}

span_σ = σ-序部分和（含空前缀 0）的 max − min。

**任务**。b = 6（7 若成本允许）：全部 (P,σ⃗) 项（ι ×2 去重可用），
N = 1..b+7：
1. F-BE-POLY-b：前 b+2 点 Lagrange 拟合（精确有理），其余
   点为保留点逐位命中——**自 N=1 起**，无 N₀ 豁免；
2. F-BE-PAR2-b：拟合多项式满足 T(−N) = (−1)^{b+1} T(N)
   （多个负点精确检验）；
3. 装配回归照旧：Σ 项（带引理 0 系数）== 已档 m_b(N)·N^{b+1}
   逐位——实现正确性门。

**判据（先记）**：三门全过 = 定理 P1/P2 在 b=6（7）的独立
实现确认。沙盒已验 b=4 全量 73 项、b=5 抽样 144 项全过。
成本：内层 N^b × 项数，b=6 与 O-BE-2 的 b=6 同级；复用
你们的分拆枚举器即可，span 计数无 Schur 调用，应更快。

## O-BE-6：数据充分性审计（优先 3，轻）

**任务**。对 b = 2..14 逐 b：在已证约束下重拟合已档精确表
（constants/tt/m_tables.json）：
1. 形式：L_b(N) = m_b(N)·N^{b+1} 为次数 ≤ b+1 的单多项式，
   且 L(−N) = (−1)^{b+1}L(N)（只保留与 b+1 同奇偶的次数项，
   未知数 = ⌊(b+1)/2⌋ + 1）；
2. 报告：可用精确点数 vs 未知数、超定余量、全部超定点逐位
   一致（F-BE-FIT-b）、拟合首项系数 == 已档 m_b 精确有理
   （F-BE-LEAD-b）；
3. 任何 b 若点数不足或不一致：如实报告，勿补算——缺口交
   数学侧定夺。

**判据（先记）**：F-BE-FIT-2..14 + F-BE-LEAD-2..14 全过 ⟹
"有限表 + 本轮定理 + l:transfer ⟹ m_b 精确有理为定理"的
数据前提落地。这是升级公告的最后一道数据门。

## 状态说明

- 在途的 O-BE-2 b=6 与 b=8 TU 抽样**转为确认层**（A/B 裁决
  已由证明取代），照常跑完回传归档；
- b=7 宇称扫描（O-BE-2 延伸）可取消——O-BE-5 的 σ-项层
  覆盖更直接。

## 落盘要求

constants/be/：ltu_scan_b{5..7}.json、sigma_scan_b{6,7}.json、
fit_audit_b2_14.json；回执 RECEIPT_R160_BE.md；logs/ 只增不改。

*Round 160 指令书。数学：Claude；纲领指挥：Hongyi Yang。*
