# Lean build record

Toolchain: leanprover/lean4:v4.33.0 (pinned in lean-toolchain); no mathlib.

Recorded builds:
- Program director's machine: lake build -> 3/3 modules (Certificate84,
  CertificateK1012, CertificateK14), zero sorry.
- Compute-side audit machine (AUDIT_R164 4.1): lake build -> 3/3 in 2.7 s,
  exit 0; axiom audit via #print axioms on one theorem per module:
      RhGate84.t222_corrected      : [propext, Classical.choice, Quot.sound]
      RhGateK1012.sigma9_recentre  : [propext, Classical.choice, Quot.sound]
      RhGateK14.sigma11_recentre   : [propext, Classical.choice, Quot.sound]
  Only the three standard axioms; no sorryAx anywhere in the tree.

Artifacts committed alongside this file (compute-side drop, O-R176-1):
- build_log.txt     verbatim from-scratch `lake build`, exit 0, with source
                    sha256 and toolchain banner.
- lake-manifest.json  as generated by that build.
- axioms_audit.txt  verbatim `#print axioms` for ALL 45 theorem declarations
                    (a superset of the three listed above; all agree).
                    Result: one distinct axiom set, zero sorryAx.
To re-verify locally:  cd lean && lake build   (then, in any module,
#print axioms <theorem>).
